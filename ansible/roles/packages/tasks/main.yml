# Distro packages. The lists live in vars/<Distribution>.yml; this role only knows how
# each package manager is driven.
- name: Install packages for {{ ansible_facts.distribution }}
  ansible.builtin.include_tasks: "{{ ansible_facts.distribution | lower }}.yml"
